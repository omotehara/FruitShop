
<h1>トップページ</h1>
<p>FruitShop へようこそ</p>
<p>サインアップまたはログインを行うことで利用できます。</p>

<h2>商品一覧</h2>

<% @products.each do |product| %>
    <% if product.photo.attached? %>
      <%= image_tag product.thumbnail %>
    <% end %>
    <br>
    <%= link_to product.name, product_path(product) %> 
    <br> 
    <%= number_to_currency(product.price, unit: "¥", precision: 0) %>
<% end %>